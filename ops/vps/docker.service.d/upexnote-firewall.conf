[Service]
# Docker recria as suas cadeias iptables ao iniciar. Reexecuta o hardening do
# UpexNote após cada start/restart, sem tornar a disponibilidade do Docker
# dependente do resultado do script de firewall.
ExecStartPost=/usr/bin/systemctl --no-block restart upexnote-firewall.service
