# UpexNote Desktop

Interface local-first do UpexNote. Será construída como uma aplicação de visual web moderna, com dark/light mode e acessibilidade desde a base.

O frontend não processa nem envia mídia por conta própria: ele conversa apenas com o worker local e mostra ao utilizador cada ação que pode usar uma API externa.
